baseURL = '{{ base_url }}'
languageCode = '{{ language_code }}'
title = '{{ site_title }}'
theme = '{{ current_theme_name }}'
copyright = '{{ site_author }} ({{ license_type }})'

[params]
  description = '{{ theme_params.description }}'
  author = '{{ site_author }}'
  showShareButtons = {{ theme_params.show_share_buttons | lower }}
  showReadingTime = {{ theme_params.show_reading_time | lower }}
  footerText = '{{ footer_text }}'
  licenseText = '{{ license_text }}'
  contactEndpoint = '{{ contact_endpoint }}'
  contactEmail = '{{ site_author_email }}'
  privacyText = '{{ privacy_vermelding }}'

# Navigatiemenu
{% for item in main_menu %}
[[menu.main]]
  name = "{{ item.name }}"
  url = "{{ item.url }}"
  weight = {{ item.weight }}
{% endfor %}

# Social Media Icons
{% for link in social_links %}
[[params.socialIcons]]
  name = "{{ link.platform }}"
  url = "{{ link.url }}"
{% endfor %}

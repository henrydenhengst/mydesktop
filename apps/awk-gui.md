install...

```bash
ansible-playbook install-awk-gui.yml -e "action=install" --ask-become-pass
```

of uninstall...

```bash
ansible-playbook install-awk-gui.yml -e "action=uninstall" --ask-become-pass
```